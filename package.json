{
  "name": "goodhabit-root",
  "version": "1.0.0",
  "description": "Root workspace for GoodHabit",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-all": "npm install --prefix server && npm install --prefix client"
  },
  "private": true
}
